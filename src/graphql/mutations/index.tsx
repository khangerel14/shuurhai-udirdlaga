import { createVehicleMutation, updateVehicleMutation } from './vehicle.mutation';
import { createVehicleManufactureMutation } from './vehicle.manufacture.mutation';
import { signInMutation, createUserMutation, updateUserMutation } from './user.mutation';
import {
  changeStatusDriver,
  createDriverMutation,
  updateDriverMutation,
  updateDriverTypeMutation,
} from './driver.mutation';

export {
  signInMutation,
  createUserMutation,
  updateUserMutation,
  changeStatusDriver,
  createDriverMutation,
  updateDriverMutation,
  createVehicleMutation,
  updateVehicleMutation,
  updateDriverTypeMutation,
  createVehicleManufactureMutation,
};
